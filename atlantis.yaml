version: 3
automerge: false
parallel_plan: true
parallel_apply: false

projects:
  - name: main-infra
    dir: terraform
    workspace: default
    autoplan:
      when_modified: ["*.tf", "../modules/**/*.tf"]
      enabled: true
    workflow: staging 

